-- 🎯 TÂM NGẮM MÀU ĐỎ DÀNH CHO MOBILE
local player = game.Players.LocalPlayer
local PlayerGui = player:WaitForChild("PlayerGui")

-- Xóa tâm cũ (nếu có)
if PlayerGui:FindFirstChild("Crosshair") then
	PlayerGui.Crosshair:Destroy()
end

-- Tạo tâm mới
local crosshair = Instance.new("Frame")
crosshair.Name = "Crosshair"
crosshair.Size = UDim2.new(0, 10, 0, 10) -- kích thước
crosshair.Position = UDim2.new(0.5, -5, 0.5, -5)
crosshair.BackgroundColor3 = Color3.fromRGB(255, 0, 0) -- màu đỏ
crosshair.BorderSizePixel = 0
crosshair.Active = false
crosshair.Parent = PlayerGui

-- Làm tròn cho đẹp
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1, 0)
corner.Parent = crosshair

-- Thêm viền đen mảnh cho dễ nhìn
local stroke = Instance.new("UIStroke")
stroke.Thickness = 1
stroke.Color = Color3.fromRGB(0, 0, 0)
stroke.Parent = crosshair
