-- SERVICES
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local LP = Players.LocalPlayer

-- DELETE OLD GUI
if game.CoreGui:FindFirstChild("STARHub") then
    game.CoreGui.STARHub:Destroy()
end

-- SAVE SYSTEM
local SaveFile = "starhub_settings.json"

local Settings = {
    AutoHit = false,
    ESP = false,
    AutoChest = false
}

if isfile and isfile(SaveFile) then
    Settings = HttpService:JSONDecode(readfile(SaveFile))
end

local function Save()
    if writefile then
        writefile(SaveFile,HttpService:JSONEncode(Settings))
    end
end

-- GUI
local gui = Instance.new("ScreenGui")
gui.Name = "STARHub"
gui.Parent = game.CoreGui

-- MAIN WINDOW
local main = Instance.new("Frame",gui)
main.Size = UDim2.new(0,420,0,260)
main.Position = UDim2.new(0.5,-210,0.5,-130)
main.BackgroundColor3 = Color3.fromRGB(20,20,20)
main.Active = true
main.Draggable = true

-- TITLE
local title = Instance.new("TextLabel",main)
title.Size = UDim2.new(1,0,0,30)
title.BackgroundColor3 = Color3.fromRGB(15,15,15)
title.Text = "⭐ STAR HUB"
title.TextColor3 = Color3.new(1,1,1)
title.Font = Enum.Font.GothamBold
title.TextSize = 16

-- TAB BUTTONS
local combatTab = Instance.new("TextButton",main)
combatTab.Size = UDim2.new(0,120,0,30)
combatTab.Position = UDim2.new(0,0,0,30)
combatTab.Text = "Combat"

local visualTab = Instance.new("TextButton",main)
visualTab.Size = UDim2.new(0,120,0,30)
visualTab.Position = UDim2.new(0,120,0,30)
visualTab.Text = "Visual"

local miscTab = Instance.new("TextButton",main)
miscTab.Size = UDim2.new(0,120,0,30)
miscTab.Position = UDim2.new(0,240,0,30)
miscTab.Text = "Misc"

-- PAGES
local combatPage = Instance.new("Frame",main)
combatPage.Size = UDim2.new(1,0,1,-60)
combatPage.Position = UDim2.new(0,0,0,60)
combatPage.BackgroundTransparency = 1

local visualPage = combatPage:Clone()
visualPage.Parent = main
visualPage.Visible = false

local miscPage = combatPage:Clone()
miscPage.Parent = main
miscPage.Visible = false

-- TAB SWITCH
combatTab.MouseButton1Click:Connect(function()
    combatPage.Visible = true
    visualPage.Visible = false
    miscPage.Visible = false
end)

visualTab.MouseButton1Click:Connect(function()
    combatPage.Visible = false
    visualPage.Visible = true
    miscPage.Visible = false
end)

miscTab.MouseButton1Click:Connect(function()
    combatPage.Visible = false
    visualPage.Visible = false
    miscPage.Visible = true
end)

-- TOGGLE BUTTON FUNCTION
local function CreateToggle(parent,text,default,callback)

    local state = default

    local btn = Instance.new("TextButton",parent)
    btn.Size = UDim2.new(0,180,0,35)
    btn.Text = text.." : "..(state and "ON" or "OFF")
    btn.BackgroundColor3 = Color3.fromRGB(40,40,40)
    btn.TextColor3 = Color3.new(1,1,1)

    btn.MouseButton1Click:Connect(function()

        state = not state
        btn.Text = text.." : "..(state and "ON" or "OFF")

        callback(state)

        Save()

    end)

    return btn
end

-- COMBAT BUTTONS
local autoHitBtn = CreateToggle(
    combatPage,
    "Auto Hit",
    Settings.AutoHit,
    function(v)
        Settings.AutoHit = v
    end
)

autoHitBtn.Position = UDim2.new(0,20,0,20)

-- VISUAL BUTTONS
local espBtn = CreateToggle(
    visualPage,
    "ESP",
    Settings.ESP,
    function(v)
        Settings.ESP = v
    end
)

espBtn.Position = UDim2.new(0,20,0,20)

-- MISC BUTTONS
local chestBtn = CreateToggle(
    miscPage,
    "Divine Chest",
    Settings.AutoChest,
    function(v)
        Settings.AutoChest = v
    end
)

chestBtn.Position = UDim2.new(0,20,0,20)

-- HIDE GUI BUTTON
local hide = Instance.new("TextButton",gui)
hide.Size = UDim2.new(0,60,0,25)
hide.Position = UDim2.new(0,10,0.5,0)
hide.Text = "HUB"

hide.MouseButton1Click:Connect(function()
    main.Visible = not main.Visible
end)